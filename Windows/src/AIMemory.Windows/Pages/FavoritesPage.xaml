<Page
    x:Class="AIMemory.Windows.Pages.FavoritesPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Padding="32" RowSpacing="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <StackPanel>
            <TextBlock Text="收藏" Style="{StaticResource PageTitleStyle}" />
            <TextBlock Text="置顶、备注、标签和继续卡片。" Opacity="0.65" />
        </StackPanel>
        <ListView x:Name="FavoritesList"
                  Grid.Row="1"
                  IsItemClickEnabled="True"
                  ItemClick="FavoritesList_ItemClick">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Border Style="{StaticResource CardStyle}" Margin="0,4">
                        <StackPanel Spacing="5">
                            <TextBlock Text="{Binding Title}" FontWeight="SemiBold" />
                            <TextBlock Text="{Binding ProjectPath}" Opacity="0.6" />
                            <TextBlock Text="{Binding Note}" TextWrapping="Wrap" />
                        </StackPanel>
                    </Border>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
